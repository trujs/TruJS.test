/**[@test({ "title": "TruJS.test.routes._EntryPoint: " })]*/
function testEntryPoint(arrange, act, assert, module) {

    arrange(function () {

    });

    act(function () {

    });

    assert(function (test) {

    });
}